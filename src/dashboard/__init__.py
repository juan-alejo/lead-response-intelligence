"""Operator dashboard — Streamlit UI for the single internal user.

Reads directly from the pipeline's SQLite store and the generated report
CSVs, so there's nothing to keep in sync; the dashboard always reflects
what the pipeline last wrote.
"""
